type (
	MemberReq {
		Id string `path:"id"`
	}

	MemberReply {
		Id   string `json:"id"`
		Name string `json:"name"`
	}
)

@server(
	prefix: api/admin
	group: member
	jwt: JwtAuth
)

service admin {
	@handler getMemberInfo
	get /member/:id (MemberReq) returns (MemberReply)
}